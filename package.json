{
  "devDependencies": {
    "vitepress": "^1.5.0"
  },
  "scripts": {
    "generate:sidebar": "node generateSidebar.mjs",
    "docs:dev": "npm run generate:sidebar && vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview"
  }
}